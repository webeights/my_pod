import 'package:flutter/material.dart';

class Car {
  final String name;
  final double price;
  final Color color;

  const Car({
    required this.name,
    required this.price,
    required this.color,
  });
}
